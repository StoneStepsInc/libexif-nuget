<?xml version="1.0" encoding="utf-8"?>
<Project DefaultTargets="Build" ToolsVersion="14.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup Label="UserMacros">
    <!-- libexif library name for all build configurations -->
    <LibExifName>libexif</LibExifName>
    <!-- Define a macro that can be used in project files to reference package directories -->
    <LibExifDir>$(MSBuildThisFileDirectory)</LibExifDir>
  </PropertyGroup>
</Project>